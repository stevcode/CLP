<RibbonWindow x:Class="Classroom_Learning_Partner.Views.MainWindowView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:views="clr-namespace:Classroom_Learning_Partner.Views"
              Icon="..\Images\InkWell.ico"
              Height="800"
              Width="1280"
              WindowStartupLocation="CenterScreen"
              WindowStyle="None"
              WindowState="Maximized"
              Closing="RibbonWindow_Closing"
              Title="{Binding Path=TitleBarText}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/MainWindowResources.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Resources/CLPBrushes.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Command="{Binding Path=SetInstructorCommand}"
                    Gesture="CTRL+SHIFT+I" />
        <KeyBinding Command="{Binding Path=SetStudentCommand}"
                    Gesture="CTRL+SHIFT+S" />
        <KeyBinding Command="{Binding Path=SetProjectorCommand}"
                    Gesture="CTRL+SHIFT+P" />
        <KeyBinding Command="{Binding Path=SetDebugCommand}"
                    Gesture="CTRL+SHIFT+D" />
        <KeyBinding Command="{Binding Path=ToggleDebugCommand}"
                    Gesture="CTRL+SHIFT+K" />
        <KeyBinding Command="{Binding Path=ToggleExtrasCommand}"
                    Gesture="CTRL+SHIFT+E" />
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <views:RibbonView Grid.Row="0"
                          DataContext="{Binding Ribbon}" />

        <!-- Control that dynamically determines what Display to show -->
        <ContentControl Grid.Row="1"
                        DataContext="{Binding SelectedWorkspace}">
            <ContentControl.Resources>

                <ControlTemplate x:Key="BlankWorkspaceTemplate">
                    <views:BlankWorkspaceView DataContext="{Binding }" />
                </ControlTemplate>

                <ControlTemplate x:Key="NotebookChooserWorkspaceTemplate">
                    <views:NotebookChooserWorkspaceView DataContext="{Binding }" />
                </ControlTemplate>

                <ControlTemplate x:Key="NotebookWorkspaceTemplate">
                    <views:NotebookWorkspaceView DataContext="{Binding }" />
                </ControlTemplate>

                <ControlTemplate x:Key="ProjectorWorkspaceTemplate">
                    <views:NotebookWorkspaceView DataContext="{Binding }" />
                </ControlTemplate>

                <ControlTemplate x:Key="UserLoginWorkspaceTemplate">
                    <views:UserLoginWorkspaceView DataContext="{Binding }" />
                </ControlTemplate>

            </ContentControl.Resources>

            <ContentControl.Style>
                <Style TargetType="{x:Type Control}">
                    <Style.Triggers>

                        <!-- BlankWorkspace trigger -->
                        <DataTrigger Binding="{Binding WorkspaceName}"
                                     Value="BlankWorkspace">
                            <Setter Property="Template"
                                    Value="{StaticResource BlankWorkspaceTemplate}" />
                        </DataTrigger>

                        <!-- NotebookChooserWorkspace trigger -->
                        <DataTrigger Binding="{Binding WorkspaceName}"
                                     Value="NotebookChooserWorkspace">
                            <Setter Property="Template"
                                    Value="{StaticResource NotebookChooserWorkspaceTemplate}" />
                        </DataTrigger>

                        <!-- NotebookWorkspace trigger -->
                        <DataTrigger Binding="{Binding WorkspaceName}"
                                     Value="NotebookWorkspace">
                            <Setter Property="Template"
                                    Value="{StaticResource NotebookWorkspaceTemplate}" />
                        </DataTrigger>

                        <!-- ProjectorWorkspace trigger -->
                        <DataTrigger Binding="{Binding WorkspaceName}"
                                     Value="ProjectorWorkspace">
                            <Setter Property="Template"
                                    Value="{StaticResource ProjectorWorkspaceTemplate}" />
                        </DataTrigger>

                        <!-- UserLoginWorkspace trigger -->
                        <DataTrigger Binding="{Binding WorkspaceName}"
                                     Value="UserLoginWorkspace">
                            <Setter Property="Template"
                                    Value="{StaticResource UserLoginWorkspaceTemplate}" />
                        </DataTrigger>

                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>

        <StatusBar Grid.Row="2"
                   VerticalAlignment="Bottom"
                   Background="{StaticResource MainColor}"
                   Height="23"
                   Padding="0"
                   Margin="0"
                   BorderThickness="0">
            <StatusBar.Resources>
                <Style BasedOn="{StaticResource StatusBarText}"
                       TargetType="{x:Type TextBlock}" />
            </StatusBar.Resources>

            <StatusBarItem>
                <TextBlock>
                    <Run Text="PAGE " />
                    <Run Text="{Binding CurrentPage.PageIndex}" />
                    <Run Text=" OF " />
                    <Run Text="{Binding NotebookPages.Count, Mode=OneWay}" />
                </TextBlock>
            </StatusBarItem>

            <StatusBarItem>
                <TextBlock>CONNECTION STATUS:</TextBlock>
            </StatusBarItem>

            <StatusBarItem>
                <TextBlock Text="{Binding OnlineStatus}" />
            </StatusBarItem>

            <StatusBarItem>

            </StatusBarItem>

        </StatusBar>

    </Grid>
</RibbonWindow>
