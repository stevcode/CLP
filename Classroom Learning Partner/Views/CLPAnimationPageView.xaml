<catel:UserControl x:Class="Classroom_Learning_Partner.Views.CLPAnimationPageView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://catel.codeplex.com"
                   xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                   xmlns:conv="clr-namespace:Classroom_Learning_Partner.Resources"
                   xmlns:views="clr-namespace:Classroom_Learning_Partner.Views">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/CLPPageResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <catel:BooleanToHidingVisibilityConverter x:Key="BooleanToHidingVisibilityConverter" />
            <conv:AnimationPresentConverter x:Key="AnimationPresentConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>

        <Viewbox Stretch="Uniform" Grid.Row="0">
            <Canvas x:Name="TopCanvas" Width="{Binding PageWidth}" Height="{Binding PageHeight}" Background="White">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseMove">
                        <catel:EventToCommand Command="{Binding MouseMoveCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                        <catel:EventToCommand Command="{Binding MouseDownCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                    <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                        <catel:EventToCommand Command="{Binding MouseUpCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <ItemsControl ItemsSource="{Binding PageObjects}" 
                              ItemContainerStyle="{StaticResource ContainerStyle}"
                              HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas x:Name="PageObjectCanvas" Background="Transparent" Cursor="{Binding PageCursor}"
                                    Width="{Binding PageWidth}" Height="{Binding PageHeight}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <views:PageObjectContainerView DataContext="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>

                <InkCanvas Strokes="{Binding InkStrokes}" Background="Transparent"
                       IsHitTestVisible="{Binding IsInkCanvasHitTestVisible}"
                       Width="{Binding PageWidth}" Height="{Binding PageHeight}"
                       EditingMode="{Binding EditingMode}"
                       EditingModeInverted="{Binding EraserMode}"
                       DefaultDrawingAttributes="{Binding DefaultDA}"
                       Cursor="{Binding PageCursor}" ForceCursor="{Binding IsUsingCustomCursors}" />

            </Canvas>
        </Viewbox>
        
        <Grid VerticalAlignment="Bottom" Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <!--Animation Present Indicator-->
            <Rectangle Grid.Row="0" HorizontalAlignment="Left" 
                     Height="20" Width="20" 
                     Stroke="Black" Fill="{Binding PageHistory.IsAnimation, Converter={StaticResource AnimationPresentConverter}}" />

            <!--Animation Progress Bar-->
            <Slider Grid.Row="0" HorizontalAlignment="Center"
                    Height="20" Width="743"
                    Minimum="0" Maximum="{Binding PageHistory.TotalHistoryTicks}"
                    Value="{Binding PageHistory.CurrentHistoryTick}" 
                    IsSnapToTickEnabled="True" TickFrequency="1.0" IsMoveToPointEnabled="True" 
                    Background="Yellow"
                    Visibility="{Binding PageHistory.IsAnimation, Converter={StaticResource BooleanToHidingVisibilityConverter}}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ValueChanged">
                        <catel:EventToCommand Command="{Binding SliderChangedCommand}"
                                              PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Slider>

            <!--Playback Controls-->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Height="30">

                <Button Width="70" Margin="0,0,10,0"
                        FontSize="12" FontWeight="Bold" Background="White" 
                        Command="{Binding ClearPageCommand}">Clear</Button>

                <Slider Minimum="0.25" Maximum="4"  
                        IsSnapToTickEnabled="True"
                        TickFrequency="0.25"
                        TickPlacement="BottomRight"
                        Width="200" Margin="10,0,10,0" 
                        Value="{Binding CurrentPlaybackSpeed}"
                        Visibility="{Binding PageHistory.IsAnimation, Converter={StaticResource BooleanToHidingVisibilityConverter}}" />

                <Button Width="70"  
                        FontSize="12" FontWeight="Bold" Background="Red" 
                        Command="{Binding RecordAnimationCommand}">Record</Button>

                <Button Width="70"  
                        FontSize="12" FontWeight="Bold" Background="Purple" 
                        Command="{Binding RewindAnimationCommand}">Rewind</Button>

                <Button Width="70" 
                        FontSize="12" FontWeight="Bold" Background="Green" 
                        Command="{Binding PlayAnimationCommand}">Play</Button>

                <Button Width="70" 
                        FontSize="12" FontWeight="Bold" Background="Gray" 
                        Command="{Binding StopAnimationCommand}">Stop</Button>

            </StackPanel>
            
        </Grid>

    </Grid>

</catel:UserControl>