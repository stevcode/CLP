<catel:DataWindow x:Class="Classroom_Learning_Partner.Views.StudentSelectorView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://catel.codeplex.com"
                  xmlns:sys="clr-namespace:System;assembly=mscorlib"
                  Title="Choose Students">
    <ItemsControl Name="StudentList" ItemsSource="{Binding SortedStudents.View, IsAsync=True}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Width="100">
                    <Run Text="{Binding FullName}"/>
                    </TextBlock>
                    <ToggleButton Width="30"
                                  Command="{Binding ElementName=StudentList, Path=DataContext.SelectStudentCommand}"
                                  CommandParameter="{Binding}"/>
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</catel:DataWindow>