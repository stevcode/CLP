<catel:UserControl x:Class="Classroom_Learning_Partner.Views.PageObjects.PageObjectContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ac="clr-namespace:AdornedControl;assembly=AdornedControl"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:conv="clr-namespace:Classroom_Learning_Partner.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="PageObjectContainerRoot"
             xmlns:catel="http://catel.codeplex.com"
             xmlns:vmpo="clr-namespace:Classroom_Learning_Partner.ViewModels.PageObjects"
             xmlns:vwpo="clr-namespace:Classroom_Learning_Partner.Views.PageObjects">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/CLPPageResources.xaml"/>              
            </ResourceDictionary.MergedDictionaries>
            <conv:LengthConverter x:Key="LengthConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="ContainerHitBox" Background="Transparent">

        <ac:AdornedControl
            x:Name="adornedControl"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            HorizontalAdornerPlacement="Outside"
            VerticalAdornerPlacement="Outside"
            >
            <ac:AdornedControl.AdornerContent >
                <!--This is the adorner-->
                <Canvas Visibility="{Binding Source={x:Static Application.Current}, Path=MainWindowViewModel.AuthoringTabVisibility}"
                    x:Name="adornerCanvas"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="{Binding Path=Width}"
                    Height="{Binding Path=Height}">

                    <Rectangle StrokeDashArray="0.5 1.0 0.3" Stroke="Black" StrokeThickness="2" RadiusX="10" RadiusY="10"
                           Canvas.Top="-5" Canvas.Left="-5"
                           Width="{Binding Converter={StaticResource LengthConverter},Path=Width, ConverterParameter=10}"
                           Height="{Binding Converter={StaticResource LengthConverter},Path=Height, ConverterParameter=10}"
                           />

                    <!--Close Button-->
                    <Button
                        x:Name="closeButton"
                        Width="40"
                        Height="40"
                        Canvas.Left="-50"
                        Canvas.Top="-25"
                        Click="closeButton_Click"
                        Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Grid
                                    x:Name="grid"
                                    >
                                    <Ellipse
                                        Stroke="Black"
                                        StrokeThickness="3"
                                        VerticalAlignment="Stretch"
                                        HorizontalAlignment="Stretch"
                                        Fill="White"
                                        />
                                    <Path 
                                        Data="M0,0 L1,1 M0,1 L1,0" 
                                        Stretch="Fill" 
                                        Stroke="Red" 
                                        StrokeThickness="3" 
                                        Margin="5" 
                                        />
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!--Drag Thumb-->
                    <Grid
                        x:Name="dragButton"
                        Canvas.Right="-50"
                        Canvas.Top="-25"
                        Width="40"
                        Height="40"
                        Cursor="ScrollAll"
                        >
                        <Ellipse
                            Stroke="Black"
                            StrokeThickness="3"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            Fill="White"
                            />
                        <Grid
                            >
                            <Path
                              Stroke="Black"
                              StrokeThickness="1"
                              Data="M 5,20 L 10,15 L 10,25 L 5,20"
                              Fill="Black"
                              />
                            <Path
                              Stroke="Black"
                              StrokeThickness="1"
                              Data="M 35,20 L 30,15 L 30,25 L 35,20"
                              Fill="Black"
                              />
                            <Path
                              Stroke="Black"
                              StrokeThickness="2"
                              Data="M 20,5 L 20,35"
                              />
                            <Path
                              Stroke="Black"
                              StrokeThickness="1"
                              Data="M 20,35 L 15,30 L 25,30 L 20,35"
                              Fill="Black"
                              />
                            <Path
                              Stroke="Black"
                              StrokeThickness="1"
                              Data="M 20,5 L 15,10 L 25,10 L 20,5"
                              Fill="Black"
                              />
                            <Path
                              Stroke="Black"
                              StrokeThickness="2"
                              Data="M 5,20 L 35,20"
                              />

                        </Grid>
                        <Thumb DragDelta="MoveThumb_DragDelta">
                            <Thumb.Template>
                                <ControlTemplate>
                                    <Rectangle
                                        Fill="Transparent"
                                        />
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Grid>

                    <!--Resize Thumb-->
                    <Grid
                        x:Name="resizeButton"
                        Canvas.Right="-50"
                        Canvas.Bottom="-50"
                        Width="40"
                        Height="40"
                        Cursor="SizeNWSE">
                        <Ellipse
                            Stroke="Black"
                            StrokeThickness="3"
                            VerticalAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            Fill="White"
                            />
                        <Grid>

                            <Path
                              Stroke="Black"
                              StrokeThickness="2"
                              Data="M 8,23 L 23,23 L 23,8"
                              />
                            <Path
                              Stroke="Black"
                              StrokeThickness="2"
                              Data="M 12,27 L 27,27 L 27,12"
                              />

                        </Grid>
                        <Thumb DragDelta="ResizeThumb_DragDelta">
                            <Thumb.Template>
                                <ControlTemplate>
                                    <Rectangle
                                        Fill="Transparent"
                                        />
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Grid>
                </Canvas>
            </ac:AdornedControl.AdornerContent>
            <Grid>
            <Rectangle Height="{Binding Path=Height, Mode=TwoWay}" Width="{Binding Path=Width, Mode=TwoWay}" Fill="Transparent" />
                <!-- Control that dynamically determines what PageObject to show -->
                <ContentControl IsHitTestVisible="True" x:Name="PageObjectView" Content="{Binding}" Height="{Binding Path=Height, Mode=TwoWay}" Width="{Binding Path=Width, Mode=TwoWay}">

                    <Control.Resources>

                        <ControlTemplate x:Key="CLPImageTemplate">
                            <vwpo:CLPImageView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPAudioTemplate">
                            <vwpo:CLPAudioView DataContext="{Binding}" />
                        </ControlTemplate>
                        
                        <ControlTemplate x:Key="CLPTextBoxTemplate">
                            <vwpo:CLPTextBoxView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPShapeTemplate">
                            <vwpo:CLPShapeView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPHandwritingRegionTemplate">
                            <vwpo:CLPHandwritingRegionView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPInkShapeRegionTemplate">
                            <vwpo:CLPInkShapeRegionView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPDataTableTemplate">
                            <vwpo:CLPDataTableView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPShadingRegionTemplate">
                            <vwpo:CLPShadingRegionView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPStampTemplate">
                            <vwpo:CLPStampView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPSnapTileContainerTemplate">
                            <vwpo:CLPSnapTileContainerView DataContext="{Binding}" />
                        </ControlTemplate>

                        <ControlTemplate x:Key="CLPStrokePathContainerTemplate">
                            <vwpo:CLPStrokePathContainerView DataContext="{Binding}" />
                        </ControlTemplate>

                    </Control.Resources>

                    <Control.Style>
                        <Style TargetType="{x:Type Control}">
                            <Style.Triggers>

                                <!-- CLPImage trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPImage">
                                    <Setter Property="Template" Value="{StaticResource CLPImageTemplate}" />
                                </DataTrigger>

                                <!-- CLPAudio trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPAudio">
                                    <Setter Property="Template" Value="{StaticResource CLPAudioTemplate}" />
                                </DataTrigger>

                                <!-- CLPTextBox trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPTextBox">
                                    <Setter Property="Template" Value="{StaticResource CLPTextBoxTemplate}" />
                                </DataTrigger>

                                <!-- CLPShape trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPShape">
                                    <Setter Property="Template" Value="{StaticResource CLPShapeTemplate}" />
                                </DataTrigger>

                                <!-- CLPHandwritingRegion trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPHandwritingRegion">
                                    <Setter Property="Template" Value="{StaticResource CLPHandwritingRegionTemplate}" />
                                </DataTrigger>

                                <!-- CLPInkShapeRegion trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPInkShapeRegion">
                                    <Setter Property="Template" Value="{StaticResource CLPInkShapeRegionTemplate}" />
                                </DataTrigger>

                                <!-- CLPDataTable trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPDataTable">
                                    <Setter Property="Template" Value="{StaticResource CLPDataTableTemplate}" />
                                </DataTrigger>

                                <!-- CLPShadingRegion trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPShadingRegion">
                                    <Setter Property="Template" Value="{StaticResource CLPShadingRegionTemplate}" />
                                </DataTrigger>

                                <!-- CLPStamp trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPStamp">
                                    <Setter Property="Template" Value="{StaticResource CLPStampTemplate}" />
                                </DataTrigger>

                                <!-- CLPSnapTileContainer trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPSnapTileContainer">
                                    <Setter Property="Template" Value="{StaticResource CLPSnapTileContainerTemplate}" />
                                </DataTrigger>

                                <!-- CLPStrokePathContainer trigger -->
                                <DataTrigger Binding="{Binding PageObjectType}" Value="CLPStrokePathContainer">
                                    <Setter Property="Template" Value="{StaticResource CLPStrokePathContainerTemplate}" />
                                </DataTrigger>

                            </Style.Triggers>
                        </Style>
                    </Control.Style>

                </ContentControl>
            </Grid>
        </ac:AdornedControl>      
    </Grid>
</catel:UserControl>
