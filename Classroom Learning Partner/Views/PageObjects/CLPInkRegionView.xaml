<UserControl x:Class="Classroom_Learning_Partner.Views.PageObjects.CLPInkRegionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Rectangle Height="{Binding Height}" Width="{Binding Width}" Fill="Transparent" Stroke="Black" StrokeThickness="4" />
        <Label Content="{Binding StoredAnswer}"></Label>
        <Label HorizontalAlignment="Right" Content="{Binding AnalysisType}"></Label>
    </Grid>
</UserControl>
