<catel:UserControl x:Class="Classroom_Learning_Partner.Views.PageObjects.CLPInkRegionView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				   xmlns:catel="http://catel.codeplex.com"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                   mc:Ignorable="d" 
                   d:DesignHeight="300" d:DesignWidth="300">

    <Grid Visibility="{Binding Source={x:Static Application.Current}, Path=MainWindowViewModel.AuthoringTabVisibility}">
        <Rectangle Height="{Binding Height}" Width="{Binding Width}" Fill="Transparent" Stroke="Gray" StrokeThickness="4" StrokeDashArray="2" />
        <Label Name="stored_answer" Margin="15" Content="{Binding StoredAnswer, Mode=TwoWay}"></Label>
        <Grid Height="50" Margin="15" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Label Name="analysis_type" VerticalAlignment="Top" Content="{Binding AnalysisType, Mode=TwoWay}"></Label>
            <Label Name="correct_answer" VerticalAlignment="Bottom" Content="{Binding CorrectAnswer, Mode=TwoWay}"></Label>
        </Grid>
        <Image Width="30" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10" Source="/Images/EditNotebook.png" MouseDown="EditInkRegion"></Image>
    </Grid>
</catel:UserControl>
