<catel:UserControl x:Class="Classroom_Learning_Partner.Views.CLPStampView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:ac="clr-namespace:AdornedControl;assembly=AdornedControl"
				   xmlns:catel="http://catel.codeplex.com"
                   xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                   xmlns:Views="clr-namespace:Classroom_Learning_Partner.Views"
                   xmlns:conv="clr-namespace:Classroom_Learning_Partner.Resources">
    <UserControl.Resources>
        <conv:LengthConverter x:Key="LengthConverter"/>
        <conv:PartsStringConverter x:Key="PartsStringConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <ac:AdornedControl HorizontalAlignment="Center" VerticalAlignment="Center" IsAdornerVisible="{Binding IsAdornerVisible, Mode=TwoWay}"
                           HorizontalAdornerPlacement="Outside" VerticalAdornerPlacement="Outside"
                       IsMouseOverShowEnabled="False">
        <ac:AdornedControl.AdornerContent >
            <!--This is the adorner-->
            <Canvas HorizontalAlignment="Center" VerticalAlignment="Center"
                        Width="{Binding Path=Width}" Height="{Binding Path=Height}">

                <Rectangle Stroke="Black" StrokeDashArray="0.5 1.0 0.3" StrokeThickness="2" 
                               RadiusX="10" RadiusY="10"
                               Canvas.Top="-5" Canvas.Left="-5"
                               Width="{Binding Converter={StaticResource LengthConverter},Path=Width, ConverterParameter=10}"
                               Height="{Binding Converter={StaticResource LengthConverter},Path=Height, ConverterParameter=10}" />

                <!--Close Button-->
                <Button x:Name="AdornerClose"
                        Click="AdornerClose_Click"
                            Width="40" Height="40"
                            Canvas.Left="-50" Canvas.Top="-25"
                            Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid>
                                <Ellipse Stroke="Black" StrokeThickness="3" Fill="White"
                                            VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                                <Path Data="M0,0 L1,1 M0,1 L1,0" 
                                          Stretch="Fill" Margin="5"
                                          Stroke="Red" StrokeThickness="3" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!--Drag Thumb-->
                <Thumb x:Name="AdornerDrag"
                           Canvas.Right="-50" Canvas.Top="-25"
                           Width="40" Height="40"
                           Cursor="ScrollAll">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="DragDelta">
                            <catel:EventToCommand Command="{Binding DragPageObjectCommand}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                        <i:EventTrigger EventName="DragStarted">
                            <catel:EventToCommand Command="{Binding DragStartPageObjectCommand}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                        <i:EventTrigger EventName="DragCompleted">
                            <catel:EventToCommand Command="{Binding DragStopPageObjectCommand}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <Thumb.Template>
                        <ControlTemplate>
                            <Grid>
                                <Ellipse Stroke="Black" StrokeThickness="3" Fill="White"
                                             VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                                <Path Stroke="Black" StrokeThickness="1" Fill="Black"
                                          Data="M 5,20 L 10,15 L 10,25 L 5,20" />
                                <Path Stroke="Black" StrokeThickness="1" Fill="Black"
                                          Data="M 35,20 L 30,15 L 30,25 L 35,20" />
                                <Path Stroke="Black" StrokeThickness="1" Fill="Black"
                                          Data="M 20,5 L 20,35" />
                                <Path Stroke="Black" StrokeThickness="1" Fill="Black"
                                          Data="M 20,35 L 15,30 L 25,30 L 20,35" />
                                <Path Stroke="Black" StrokeThickness="1" Fill="Black"
                                          Data="M 20,5 L 15,10 L 25,10 L 20,5" />
                                <Path Stroke="Black" StrokeThickness="1" Fill="Black"
                                          Data="M 5,20 L 35,20" />
                            </Grid>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

                <!--Resize Thumb-->
                <Thumb x:Name="AdornerResize"
                           Canvas.Right="-50" Canvas.Bottom="-50"
                           Width="40" Height="40"
                           Cursor="SizeNWSE">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="DragDelta">
                            <catel:EventToCommand Command="{Binding ResizePageObjectCommand}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                        <i:EventTrigger EventName="DragStarted">
                            <catel:EventToCommand Command="{Binding ResizeStartPageObjectCommand}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                        <i:EventTrigger EventName="DragCompleted">
                            <catel:EventToCommand Command="{Binding ResizeStopPageObjectCommand}" PassEventArgsToCommand="True" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <Thumb.Template>
                        <ControlTemplate>
                            <Grid>
                                <Ellipse Stroke="Black" StrokeThickness="3" Fill="White"
                                             VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                                <Path Stroke="Black" StrokeThickness="1"
                                          Data="M 8,23 L 23,23 L 23,8" />
                                <Path Stroke="Black" StrokeThickness="1"
                                          Data="M 12,27 L 27,27 L 27,12" />
                            </Grid>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>

            </Canvas>
        </ac:AdornedControl.AdornerContent>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Thumb Grid.Row="0" 
                   MouseEnter="StampHandleHitBox_MouseEnter"
                   MouseLeave="StampHandleHitBox_MouseLeave">
                <Thumb.Template>
                    <ControlTemplate>
                        <Polygon x:Name="StampHandleHitBox"  
                                 Width="{Binding Width}" MinWidth="50"  
                                 Height="{Binding PageObject.HANDLE_HEIGHT}" Stretch="Fill"
                                 HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0"
                                 Stroke="Black" StrokeThickness="0" Fill="{Binding StampHandleColor}" 
                                 Points="0,0 3,0 2,1 1,1" >
                        </Polygon>
                    </ControlTemplate>
                </Thumb.Template>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="DragDelta">
                        <catel:EventToCommand Command="{Binding Path=DragStampCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="DragStarted">
                        <catel:EventToCommand Command="{Binding Path=CopyStampCommand}"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="DragCompleted">
                        <catel:EventToCommand Command="{Binding Path=PlaceStampCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Thumb>
            <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2"
                    Width="{Binding Width, Mode=TwoWay}" 
                    Height="{Binding StrokePathContainer.Height, Mode=TwoWay}" 
                    VerticalAlignment="Top" Margin="0">
                <Views:CLPStrokePathContainerView DataContext="{Binding StrokePathContainer}" 
                                                  Width="{Binding Width, Mode=TwoWay}" 
                                                  Height="{Binding Height, Mode=TwoWay}" />
            </Border>
            <Grid Grid.Row="2" Visibility="{Binding PartsRegionVisibility}">
                
                <Border x:Name="PartsHandwritingBox"
                            BorderBrush="Black" BorderThickness="0,0,0,2"
                            Width="{Binding HandwritingRegionParts.Width}"
                            Height="{Binding HandwritingRegionParts.Height}"
                            HorizontalAlignment="Center" Margin="0" IsHitTestVisible="False">
                    <Views:CLPHandwritingRegionView DataContext="{Binding HandwritingRegionParts}" 
                                                Width="{Binding Width, Mode=TwoWay}" 
                                                Height="{Binding Height, Mode=TwoWay}"
                                                IsHitTestVisible="False" />
                </Border>
                <TextBlock x:Name="PartsAuto"
                       FontFamily="SketchFlow Print" FontSize="72"
                       Width="{Binding Width}"
                       Height="{Binding HandwritingRegionParts.Height}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       VerticalAlignment="Bottom"
                       Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=PageObject.PartsAutoGenerated, Mode=TwoWay}">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource PartsStringConverter}" ConverterParameter="">
                            <Binding Path="PageObject.Parts" />
                            <Binding Path="PageObject.PartsInterpreted" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock x:Name="PartsDetected"
                       FontFamily="SketchFlow Print" FontSize="24"
                       Width="{Binding PageObject.DETECTED_PARTS_SIDE}"
                       Height="{Binding PageObject.DETECTED_PARTS_SIDE}"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       TextAlignment="Left"
                       Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=PageObject.PartsInterpreted, Mode=TwoWay}">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource PartsStringConverter}" ConverterParameter="">
                            <Binding Path="PageObject.Parts" />
                            <Binding Path="PageObject.PartsInterpreted" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <Rectangle x:Name="PartsHitBox" Fill="Transparent"
                        Width="{Binding PageObject.DETECTED_PARTS_SIDE}"
                        Height="{Binding PageObject.DETECTED_PARTS_SIDE}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDown">
                            <catel:EventToCommand Command="{Binding Path=ShowKeyPadCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Rectangle>
            </Grid>
        </Grid>

    </ac:AdornedControl>

</catel:UserControl>
