<catel:UserControl x:Class="Classroom_Learning_Partner.Views.PageObjects.CLPSnapTileContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:catel="clr-namespace:Catel.Windows.Controls;assembly=Catel.Windows"
             xmlns:vmo="clr-namespace:Classroom_Learning_Partner.ViewModels.PageObjects"
             x:TypeArguments="vmo:CLPSnapTileContainerViewModel">
    <ListView ItemsSource="{Binding Path=Tiles}" Height="{Binding Path=Height}" Width="54" Background="Azure" BorderThickness="0" Margin="0" Padding="0">
        <ListView.ItemTemplate>
            <DataTemplate>
                <Rectangle Fill="SpringGreen" Stroke="Black" StrokeThickness="2" Margin="0" Height="48" Width="48" />
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>
</catel:UserControl>
