<catel:UserControl x:Class="Classroom_Learning_Partner.Views.MirrorDisplayView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:sys="clr-namespace:System;assembly=mscorlib"
                   xmlns:catel="http://catel.codeplex.com"
                   xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                   xmlns:conv="clr-namespace:Classroom_Learning_Partner.Converters"
                   xmlns:views="clr-namespace:Classroom_Learning_Partner.Views"
                   Background="DeepPink">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Resources/CLPBrushes.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <conv:TypeToStringConverter x:Key="TypeToStringConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" 
                   Text="{Binding CurrentPage.Submitter.FullName}"              
                   Foreground="{StaticResource MainColor}"   
                   FontWeight="Bold" VerticalAlignment="Bottom">
            <TextBlock.LayoutTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-90"/>
                    <TranslateTransform/>
                </TransformGroup>
            </TextBlock.LayoutTransform>
        </TextBlock>
        
        <Border Grid.Column="1" 
                BorderThickness="{Binding BorderThickness}" BorderBrush="{StaticResource GrayBorderColor}" 
                Width="{Binding BorderWidth}" Height="{Binding BorderHeight}">
            <ScrollViewer x:Name="MirrorDisplayScroller" 
                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto"
                          Padding="0,0,8,0" Margin="0,0,-33,0">
                <ScrollViewer.Resources>
                    <sys:Double x:Key="{x:Static SystemParameters.VerticalScrollBarWidthKey}">25</sys:Double>
                </ScrollViewer.Resources>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="ScrollChanged">
                        <catel:EventToCommand Command="{Binding PageScrollCommand}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Grid Width="{Binding DimensionWidth}" Height="{Binding DimensionHeight}">

                    <Control>
                        <Control.Resources>

                            <ControlTemplate x:Key="CLPPageTemplate">
                                <views:CLPPageView DataContext="{Binding CurrentPage}" />
                            </ControlTemplate>

                            <ControlTemplate x:Key="CLPAnimationPageTemplate">
                                <views:CLPAnimationPageView DataContext="{Binding CurrentPage}" />
                            </ControlTemplate>

                        </Control.Resources>

                        <Control.Style>
                            <Style TargetType="{x:Type Control}">
                                <Style.Triggers>

                                    <!-- CLPPage trigger -->
                                    <DataTrigger Binding="{Binding CurrentPage, Converter={StaticResource TypeToStringConverter}}" Value="CLPPage">
                                        <Setter Property="Template" Value="{StaticResource CLPPageTemplate}" />
                                    </DataTrigger>

                                    <!-- CLPAnimationPage trigger -->
                                    <DataTrigger Binding="{Binding CurrentPage, Converter={StaticResource TypeToStringConverter}}" Value="CLPAnimationPage">
                                        <Setter Property="Template" Value="{StaticResource CLPAnimationPageTemplate}" />
                                    </DataTrigger>

                                </Style.Triggers>
                            </Style>
                        </Control.Style>
                    </Control>

                </Grid>
            </ScrollViewer>
        </Border>
        
    </Grid>

</catel:UserControl>
