<Window x:Class="Classroom_Learning_Partner.Views.Modal_Windows.NotebookNamerWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotebookNamerWindowView" 
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" FocusManager.FocusedElement="{Binding ElementName=NotebookName}" Margin="10">
            <Label VerticalContentAlignment="Center" Height="50" FontSize="15" FontWeight="Bold">Choose Name for Notebook:</Label>
            <TextBox VerticalContentAlignment="Center" x:Name="NotebookName" Width="200" Height="25" FontSize="15"></TextBox>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Button IsDefault="True" Content="OK" Width="100" Height="30" Margin="5" Click="OKButton_Click"/>
            <Button IsCancel="True" Content="Cancel" Width="100" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
