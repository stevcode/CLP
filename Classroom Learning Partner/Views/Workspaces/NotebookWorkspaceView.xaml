<catel:UserControl x:Class="Classroom_Learning_Partner.Views.Workspaces.NotebookWorkspaceView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://catel.codeplex.com"
                   xmlns:vmw="clr-namespace:Classroom_Learning_Partner.ViewModels.Workspaces"
                   xmlns:vwd="clr-namespace:Classroom_Learning_Partner.Views.Displays"
                   xmlns:vw="clr-namespace:Classroom_Learning_Partner.Views">

    <UserControl.Resources>
        <catel:BooleanToCollapsingVisibilityConverter x:Key="BoolConverter"/>
        <Style TargetType="{x:Type ContentControl}" x:Key="DisplayStyle">
            <Style.Triggers>

                <!-- LinkedDisplay trigger -->
                <DataTrigger Binding="{Binding SelectedDisplay.DisplayName}" Value="LinkedDisplay">
                    <Setter Property="Content">
                        <Setter.Value>
                            <vwd:LinkedDisplayView DataContext="{Binding SelectedDisplay}" />
                        </Setter.Value>
                    </Setter>
                </DataTrigger>

                <!-- GridDisplay trigger -->
                <DataTrigger Binding="{Binding SelectedDisplay.DisplayName}" Value="GridDisplay">
                    <Setter Property="Content">
                        <Setter.Value>
                            <vwd:GridDisplayView Height="500" Width="500"  DataContext="{Binding SelectedDisplay}" />
                        </Setter.Value>
                    </Setter>
                </DataTrigger>

            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <!-- Content -->
    <Grid Background="{Binding WorkspaceBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <vw:SideBarView DataContext="{Binding}"  Grid.Column="0" Visibility="{Binding Source={x:Static Application.Current}, Path=MainWindowViewModel.SideBarVisibility, Converter={StaticResource BoolConverter}}" />

        <!-- Control that dynamically determines what Display to show -->
        <ContentControl Grid.Column="1" Style="{StaticResource DisplayStyle}"  x:Name="DisplayView"  />


    </Grid>
</catel:UserControl>
