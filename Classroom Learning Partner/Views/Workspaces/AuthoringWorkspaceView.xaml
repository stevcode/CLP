<UserControl x:Class="Classroom_Learning_Partner.Views.Workspaces.AuthoringWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vw="clr-namespace:Classroom_Learning_Partner.Views"
             xmlns:dvw="clr-namespace:Classroom_Learning_Partner.Views.Displays"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid x:Name="MainGrid" Background="Salmon">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <vw:SideBarView  Grid.Column="0" DataContext="{Binding Path=SideBar}"/>
            
            <dvw:SinglePageDisplayView Grid.Column="1" Content="{Binding Path=Display}" Margin="5"/>

        </Grid>
    </Grid>
</UserControl>
