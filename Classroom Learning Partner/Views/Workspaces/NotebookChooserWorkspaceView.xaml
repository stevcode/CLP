<catel:UserControl x:Class="Classroom_Learning_Partner.Views.NotebookChooserWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:catel="http://catel.codeplex.com"
             xmlns:Views="clr-namespace:Classroom_Learning_Partner.Views"
             Background="CadetBlue">

    <Grid x:Name="grid">
        <ListBox ItemsSource="{Binding Path=NotebookNames}" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.SelectNotebookCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Views:NotebookChooserWorkspaceView}}}" CommandParameter="{Binding}" 
                            Margin="10" Height="90" Width="175">
                        <DockPanel LastChildFill="True">
                            <Label DockPanel.Dock="Bottom" Height="30" 
                                   Content="{Binding}" FontWeight="Bold" FontSize="12"
                                   HorizontalContentAlignment="Center" VerticalContentAlignment="Bottom"/>
                            <Image Margin="5" Source="..\..\Images\NotebookImage.png" />
                        </DockPanel>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel  Margin="20" Width="{Binding ElementName=grid,Path=ActualWidth}" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</catel:UserControl>