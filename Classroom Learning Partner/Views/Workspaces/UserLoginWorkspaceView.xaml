<catel:UserControl x:Class="Classroom_Learning_Partner.Views.UserLoginWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:catel="http://catel.codeplex.com"
             xmlns:Views="clr-namespace:Classroom_Learning_Partner.Views"
             Background="CadetBlue">
    <Grid x:Name="grid">
        <ListBox ItemsSource="{Binding Path=UserNames}" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.LogInCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Views:UserLoginWorkspaceView}}}" CommandParameter="{Binding}" 
                            Margin="10" Height="125" Width="125" Content="{Binding}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel  Margin="20" Width="{Binding ElementName=grid,Path=ActualWidth}" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</catel:UserControl>
