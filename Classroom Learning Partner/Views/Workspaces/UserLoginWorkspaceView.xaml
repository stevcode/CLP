<catel:UserControl x:Class="Classroom_Learning_Partner.Views.Workspaces.UserLoginWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wvw="clr-namespace:Classroom_Learning_Partner.Views.Workspaces"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Background="CadetBlue"
             xmlns:catel="http://catel.codeplex.com"
             xmlns:vmw="clr-namespace:Classroom_Learning_Partner.ViewModels.Workspaces"
             x:TypeArguments="vmw:UserLoginWorkspaceViewModel">
    <ListBox ItemsSource="{Binding Path=UserNames}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Button Command="{Binding DataContext.LogInCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type wvw:UserLoginWorkspaceView}}}" CommandParameter="{Binding}" Margin="10" x:Name="UserNameButton"  Height="125" Width="125" Content="{Binding}" />
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Margin="20" Width="{Binding RelativeSource={RelativeSource AncestorType=Window},Path=ActualWidth}" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
    </ListBox>
</catel:UserControl>
