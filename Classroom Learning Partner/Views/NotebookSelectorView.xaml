<catel:UserControl x:Class="Classroom_Learning_Partner.Views.NotebookSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:catel="clr-namespace:Catel.Windows.Controls;assembly=Catel.Windows"
             xmlns:vm="clr-namespace:Classroom_Learning_Partner.ViewModels"
             x:TypeArguments="vm:NotebookSelectorViewModel">
    <Grid MaxHeight="300" MaxWidth="270" Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Background="Transparent" Command="{Binding Path=SelectNotebookCommand}">
            <Image Source="..\Images\NotebookImage.png" />
        </Button>
        <Label Grid.Row="1" Height="30" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Content="{Binding Path=NotebookName}" FontWeight="ExtraBold" FontSize="14" />
    </Grid>
</catel:UserControl>
